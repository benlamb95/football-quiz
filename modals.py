class Question:
    """
    Class for question which provides both,
    question asked and answer
    """
    def __init__(self, prompt, answer):
        self.prompt = prompt
        self.answer = answer
